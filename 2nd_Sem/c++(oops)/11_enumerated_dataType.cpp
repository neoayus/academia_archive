#include<iostream>
using namespace std ;

//this is another user defined data type which provides a way for attaching names to numbers, thereby increasing comprehensibility of code.
// the enum keyword enumerates a list of words by assigning them values 0,1,2,3.. and so on.
enum shape{circle,square,triangle,rectangle} ; 
enum colors{red,green,blue,cyan,black,white} ; 
enum position{on,off} ;



int main(){
    shape elipse ; 
    colors megenta ; // these are the variables of type shape nd color. 
    // enumerated datatype is usually used to define symbolic constants for switch 
    
    return 0 ; 
}
