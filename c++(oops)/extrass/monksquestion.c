#include<stdio.h>
int main(){
    int size, rotations; 
    printf("Array should be of size :");
    scanf("%d",&size); 
    printf("Array will be rotated ? times :");
    scanf("%d",&rotations);
    
    int nums[size]; 
    

    return 0 ;
}
