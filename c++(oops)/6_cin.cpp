#include<iostream>
using namespace std ; 

int main(){
    int num1 ; 
    cout << "input an integer : " ; 
    cin >> num1 ; 
    // this operator is known as 'extraction' or 'get from' operator. 
    // it extracts value form keyboard and assign in to the variable on right. 
    
    return 0 ; 
}